

def compute_correlation():
    ''' 
    Compute correlation of MMD for different kernel/hyperparameter
    combinations and return the kernel/hyperparameter combination with
    the best correlation (?) 

    Parameters:
    ______________
    perturbation_values: list of perturbation levels
    perturbed_graphs: list of list of perturbed graphs 
    correlation_type: type of correlation to calculate (pearsons, spearmons)
    candidate_kernels: list of kernels to consider
    candidate_descriptor_functions: list of descriptor functions to use
    candidate_sigma: list of sigma to iterate over

    Returns:
    ______________
    kernel and parameter: kernel-parameter combination with the best correlation
    mmd: the computed MMD using the optimal settings


    '''

    pass
